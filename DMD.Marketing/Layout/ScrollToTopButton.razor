@rendermode @(new InteractiveServerRenderMode(prerender: false))

<MudScrollToTop>
    <MudFab Color="Color.Primary"
            StartIcon="@Icons.Material.Filled.KeyboardArrowUp"
            Size="Size.Medium"
            Style="box-shadow:0 4px 24px rgba(25,118,210,0.45);" />
</MudScrollToTop>
